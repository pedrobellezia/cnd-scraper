services:
  app:
    build: .
    container_name: cnd-scrapper
    ports:
      - "${PORT}:${PORT}"
      - "${VNC_PORT:-5900}:5900"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./screenshots:/app/screenshots

